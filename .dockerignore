# Git / IDE
.git
.github
.husky
.vscode

# Build outputs (host) — never send into Docker context
.next
out
dist
coverage
.turbo
*.tsbuildinfo

# Dependencies
node_modules

# Local env and docs (image gets env from compose / runtime, not baked from .env)
.env
.env.*
!.env.example

# Repo meta (not needed to build the app)
.gitignore
LICENSE
README.md
docker-compose.yml
Dockerfile
caddy/
